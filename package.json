{
  "name": "mapaflex-pwa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo Cloudflare Worker static assets ready",
    "deploy": "wrangler deploy",
    "deploy:cloudflare": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
