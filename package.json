{
  "name": "mapaflex-pwa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo Cloudflare Worker static build",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
