{
  "name": "mapaflex-pwa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo Vercel static build ready",
    "deploy:cloudflare": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
